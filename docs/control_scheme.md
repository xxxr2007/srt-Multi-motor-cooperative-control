# 多电机协作（同步）控制仿真方案

## 1. 研究问题

多电机驱动装备中，各电机参数存在制造离散性（出厂体质差异）
(惯量、摩擦 ±10% 摄动)，负载也会突变。协作控制的目标：让各电机转速保持同步，
不因参数不一致或单机负载突变而"掉队"，同时尽量不影响整体转速跟随。

本仿真回答的问题：主从、交叉耦合、偏差耦合三种经典同步策略，在参数摄动 +
突加负载 + 周期波动负载工况下，谁的同步性能最好？代价是什么？
进一步地：在最强经典策略上叠加扰动观测器(DOB)前馈，能把同步性能再推高多少？
<!--
### 多电机同步控制策略简述
 1. 主从：从机单向跟踪主机，结构简单但从机受扰时主机不等待
 2. 交叉耦合：相邻电机互传误差互相补偿，兼顾跟踪与两两同步
 3. 偏差耦合：所有电机共享全局速度差进行协同补偿，同步抗扰性能最优

### 扰动观测器（DOB）​
用模型反推出真实作用在系统上的未知扰动，再提前抵消它。

### 前馈与后馈
反馈看结果纠错，前馈看预测提前补

### 利用工具：Markdown 
一种“用纯文本写格式”的轻量标记语言（下面代码都是利用的Markdowm）
# 标题	    大标题	
**加粗**	  加粗	
1. 列表	    有序列表
| 表 |	    表格	
$...$	      行内公式，渲染后显示斜体的
 -->

## 2. 被控对象模型

单台电机采用机电简化模型（转速环 + 转矩饱和），第 $i$ 台：
<!-- 后续考虑建模电流环、反电动势等细节 -->
<!--两个 $ 表示"从这里开始是公式"，渲染后是居中独立公式 -->
<!--  \frac{d\omega_i}{dt} 表示的是角加速度，\frac{分子}{分母} -->
$$
J_i \frac{d\omega_i}{dt} = T_{e,i} - B_i \omega_i - T_{L,i}
$$ 

| 符号 | 含义 | 取值（1/2/3/4 号电机） |
|---|---|---| 
| $J_i$ | 转动惯量 (kg·m²) |  0.010 / 0.011 / 0.0095/0.0095 |
| $B_i$ | 粘性摩擦 (N·m·s/rad)（摩擦系数） | 0.0012 / 0.0011 / 0.0013/0.0013 |
| $T_{e,i}$ | 电磁转矩，限幅 ±5 N·m（这个不是给定参数，控制器算出来） | 转速环 PI 输出 |
| $T_{L,i}$ | 负载转矩（随工况变化） | 见工况 |
<!-- J_i中的_表示下标 -->
转速环 PI（含条件抗积分饱和）：
<!--条件抗积分饱和指转矩饱和时暂停积分累积，防止积分器越积越多导致退饱和后严重超调  -->
$$
T_{e,i} = \mathrm{sat}_{\pm T_{max}}\left[K_p(\omega_{ref,i} - \omega_i) + K_i \int (\omega_{ref,i} - \omega_i)\,dt\right]
$$

<!-- \mathrm{} 让字母变正体（函数名习惯不用斜体）
\left[ \right] 是自适应大小的括号
\pm 指的是正负号
T_{e,i} = \mathrm{sat}_{\pm T_{max}}\left[ ... \right]
饱和函数：方括号里的东西算出来后，超过 ±5 N·m 就掐到 ±5。
K_p(\omega_{ref,i} - \omega_i)
比例项：给定转速 − 实际转速 = 误差，乘增益
​K_i \int(\omega_{ref,i} - \omega_i)\,dt	
积分项：误差对时间累积，\int 是积分号，\,dt 里的 \, 是一个小空格（纯排版美观），dt 是积分变量
 -->
其中 $K_p = 0.8\ \mathrm{N\cdot m/(rad/s)}$，$K_i = 30\ \mathrm{N\cdot m/rad}$。
<!-- \ 是在公式里面插入空格
两个增益单位不同
\cdot 是乘点
 -->

**推导量纲**（自查用）：$K_p\cdot e$ 的单位是 (N·m/(rad/s))·(rad/s) = N·m ✓；
$K_i$ 的单位是 N·m / rad，积分后 ∫e dt 单位 rad，乘起来 N·m ✓。

## 3. 负载工况

| 时段 | 事件 |
|---|---|
| 0 – 0.5 s | 给定转速斜坡 0 → 120 rad/s（软启动） |
| t = 1.2 s | 电机 2 突加 1.8 N·m 恒负载（模拟单机卡滞） |
| t ∈ [1.8, 2.6] s | 电机 4 线性斜坡加载 0 → 1.0 N·m 并保持（渐变负载） |
| t ≥ 2.0 s | 电机 3 施加 0.4·sin(2π·2t) N·m 周期波动负载 |

四种扰动各占一台电机，覆盖"无扰 / 突变 / 渐变 / 周期"完整谱系，
台数扩展时按 `N_MOTOR` 与参数表同步增删即可。

## 4. 三种同步策略

记 $n=4$，$\omega^*$ 为给定转速。所有策略的**区别只在参考转速怎么修正**，
转速环 PI 结构完全相同——这样对比才公平。

### 4.1 主从控制（Master-Slave）

1 号电机为主机跟踪给定，其余跟踪**主机的实际转速**：

$$
\omega_{ref,1} = \omega^*, \qquad \omega_{ref,j} = \omega_1 \ (j = 2,\dots,n)
$$
<!-- \qquad 大空格
\dots 省略号
\omega_{ref,1} 主机的w
\omega_{ref,j} 从机的w
带*的是给定的值，即主机的转速是给定的指令，但是主机会被负载拉低转速，导致主机的实际转速会降低，从机的参照是主机的实际转速
 -->
- 优点：结构最简单，各从机互不干扰。
- 缺点：从机响应滞后主机一个跟踪误差；**主机受扰会传染给所有从机**，且从机受扰不会反馈给主机。

### 4.2 交叉耦合控制（Cross-Coupling, CCC）

每台电机计算自己与"其他电机平均转速"之差（同步误差），补偿到自己的参考转速上：

$$
\varepsilon_i = \omega_i - \frac{1}{n-1}\sum_{j \neq i} \omega_j, \qquad
\omega_{ref,i} = \omega^* - \left(K_s \varepsilon_i + K_{si}\int \varepsilon_i\, dt\right)
$$
<!-- 
\varepsilon_i                    第 i 台的同步误差
K_s \varepsilon_i                同步补偿的比例项
 K_{si}\int \varepsilon_i\, dt   同步补偿的积分项
其他电机的平均转速：\sum_{j \neq i} 是"对 j 求和但排除自己”，共 n−1 台，所以除以 n−1
 -->
符号解读：若 $i$ 号比大家快（$\varepsilon_i > 0$），补偿为负，压低它的参考转速；
反之抬高。**单机受扰时全场一起"扶"它，用平均转速的暂时下跌换同步。**

### 4.3 惯量加权偏差耦合控制（Deviation Coupling, DCC）

对**每一对**电机偏差分别补偿，把对方惯量占比作为权重（大惯量电机的偏差更"可信"）：
<!-- 惯量指电机转子及连接轴在旋转时抵抗角加速度（即转速改变）的转动惯量，通常用字母 $J_m$ 或 $J_r$ 表示-->
$$
\omega_{ref,i} = \omega^* - \sum_{j \neq i} w_{ij}\left(K_s(\omega_i - \omega_j) + K_{si}\int (\omega_i - \omega_j)\,dt\right),
\qquad w_{ij} = \frac{J_j}{\sum_{k \neq i} J_k}
$$
<!-- \neq 即不等于 
w_{ij} 惯量权重
K_s(\omega_i - \omega_j) + K_{si}\int (\omega_i - \omega_j)\,dt 每一对电机的两两偏差
J_j 对方惯量，两两互相
\sum_{k \neq i} J_k其他所有电机的惯量之和
-->
与 CCC 的区别：CCC 只对"与平均的差"用一个补偿通道；DCC 对每对偏差各有一个
积分器，且加权后响应更偏向跟随高惯量电机。

### 4.4 偏差耦合 + 扰动观测器前馈（DCC+DOB，本项目创新策略）

前三种策略都是**纯反馈**：误差已经出现了，转速环 PI 才被动调节。
本策略在 DCC 骨架上给每台电机再配一个**扰动观测器**，在线估计"集总扰动"
并**前馈抵消**——不等误差出现就先把扰动补掉，反馈环只负责收拾估计残差。

**①集总扰动**：用名义参数 $J_n$、$B_n$（取全组均值）改写机械方程
 <!-- 名义参数：假装参数没摄动的平均值
 改写机械方程：就是把真实方程里的J、B换成 J_n,B_n,​多出来的差额打包记为d  -->
$$
J_n \dot{\omega} = T_e - B_n \omega - d, \qquad
d = T_L + (J - J_n)\dot{\omega} + (B - B_n)\omega
$$
<!-- \dot 求导
$d$：系统集总扰动，将外部扰动与内部参数摄动等效为一个统一的转矩扰动量。
$T_L$：外部负载转矩。
$J$：电机与负载的实际转动惯量（随着机械结构运动、负载变化而发生摄动）。
$J_n$：控制器中预设的标称转动惯量。
$J = J - J_n$：惯量不确定性（摄动量）。
$B$：系统实际粘滞摩擦系数。
$B_n$：控制器中预设的标称粘滞摩擦系数。
$B = B - B_n$：阻尼/摩擦系数不确定性（摄动量）。
$\omega$：电机机械角速度。
$\dot{\omega}$：角加速度（角速度对时间的一阶导数）。
 -->
真实负载与 ±10% 参数摄动的影响全被"吸"进 $d$：估计并抵消 $d$，
等价于对**负载扰动和参数失配同时鲁棒**。
<!-- 鲁棒：指系统在面临参数摄动、外部扰动或未建模动态等不确定性因素时，依然能够维持既定性能或稳定运行的能力。 -->

**② 观测器构造（关键是避开微分）**：反解 $d = T_e - B_n\omega - J_n\dot{\omega}$
需要微分转速，数值噪声下不可用，故串联一阶低通 $Q(s) = \dfrac{g}{s+g}$
（$g$ 为观测器带宽）：
<!-- 实测转速永远带测量噪声，微分是噪声放大器——平滑曲线上一点点毛刺，微分后被放大成大幅抖动。仿真里数值误差同理。所以这条“直算”的路走不通。 
Q(s) 滤波器的传递函数（s 域写法）
\dfrac 是显示模式的大分数
g	截止频率/带宽
-->

$$
\hat{d} = Q(s)\left(T_e - B_n\omega - J_n s\omega\right)
$$

利用恒等式 $sQ(s) = g\left(1 - Q(s)\right)$ 消去微分项：

$$
\hat{d} = Q(s)\left(T_e + (gJ_n - B_n)\omega\right) - gJ_n\omega
$$

写成状态方程即代码实现（$x$ 为滤波器状态）：

$$
u = T_e + (gJ_n - B_n)\omega, \qquad
\dot{x} = g(u - x), \qquad
\hat{d} = x - gJ_n\omega
$$

稳态自检（$s \to 0$ 时 $Q \to 1$、$x \to u$）：$\hat{d} = T_e - B_n\omega = d$
（此时 $\dot{\omega} = 0$）✓ 无静差。
<!-- \to 用于生成向右的箭头，等价于 \rightarrow。
\hat 用于在字母正上方添加一个尖帽符号，在状态观测器（如状态观测器、卡尔曼滤波、DOB、ESO）中，字母上方带尖帽统一表示该状态的估计值，用来与真实值区分。
 -->
**③ 前馈补偿与两条实现纪律**：

$$
T_e^{cmd} = \mathrm{sat}_{\pm T_{max}}\left( T_e^{PI} + \hat{d}_{k-1} \right)
$$

- 观测器输入 $T_e$ 必须取**限幅后实际施加**的转矩，否则饱和期间观测器以为
  电机输出了并没有输出的转矩，估计被系统性估偏（观测器层面的"抗饱和"）；
- 前馈用**上一拍**的 $\hat{d}$：本拍转矩未施加前本拍估计根本算不出来，硬用
  构成代数环；0.1 ms 延迟相对观测器时间常数 $1/g = 10$ ms 可以忽略。

**④ 带宽整定**：取 $g = 100$ rad/s（≈15.9 Hz）。
显式欧拉下 $g\Delta t = 0.01 \ll 1$，
<!-- \Delta 差值 -->
稳定余量充足；约为最快扰动的 8 倍，
该频率下幅值衰减 <1%、相位滞后 ≈7°；与转速环带宽保持 5–10 倍时标分离。

**实测效果**（2 号机突加 1.8 N·m）：$\hat{d}$ 经约 $5/g = 50$ ms 收敛，
稳态值 $1.7895 = 1.8 - 0.0105$，其中 $-0.0105$ 恰为 2 号机摩擦失配项
$(B_2 - B_n)\omega^*$——观测器把负载和参数失配一并估计了出来
（见 `results/dob_observer.png`）。

## 5. 评价指标

| 指标 | 定义 | 说明 |
|---|---|---|
| 同步误差 | $\max_{i,j}\|\omega_i - \omega_j\|$ | 同型电机下等价于 max − min |
| 同步误差 RMS | 升速结束后全程均方根 | 反映整体同步水平 |
| 突加载峰值误差 | t ∈ [1.2, 1.8] s 内最大同步误差 | 抗扰能力 |
| 恢复时间 | 突加后误差回落到 0.5 rad/s 内且连续保持 50 ms | 快速性 |
| 电机 2 最大转速跌落 | 突加后电机 2 最低点与给定之差 | 跟随性代价 |

## 6. 预期现象（用于自查仿真对不对）

1. 升速段：主从的从机滞后主机 → 同步误差不为零；CCC/DCC 耦合通道会主动"拉齐"。
2. 突加载：主从中只有电机 2 自己扛，误差峰值大、恢复慢；CCC/DCC 其他电机转速
   会跟着略降（平均转速牺牲），但**相对**同步误差更小。
3. DCC 与 CCC 接近，加权后对最小惯量电机的波动负载响应略有差别。
4. DCC+DOB：扰动出现的瞬间前馈已就位，突加载峰值误差被压到恢复判定阈值
   （0.5 rad/s）以下，恢复时间归零；周期负载段的同步波动也被显著抹平——
   观测器对正弦扰动同样能有效跟踪。

## 7. 后续工作

- [ ] 迁移到 Simulink/Simscape Electrical（PMSM + 变频器级模型），见 `electrical/matlab/multi_motor_sync_matlab.m`
- [ ] 同步 PI 参数扫描（K_s、K_si 网格），找最优组合
- [ ] 加入 CAN 总线通信延迟（0.5–2 ms）对同步误差的影响
- [x] 复合创新策略 DCC+DOB：偏差耦合 + 扰动观测器前馈（2026-09-19 完成，四项指标全面最优）

## 参考

- 张纯江等. 锂电池与超级电容混合储能系统及其能量管理策略综述. 电工技术学报, 2018.（多源协调思想）
- Koren Y. Cross-coupled computer control for manufacturing systems. ASME J. Dyn. Syst., 1980.（交叉耦合起源）
- Perez-Pinal F J, et al. Relative coupling strategy for multi-motor synchronization. IEEE IAS, 2003.（偏差/相对耦合）
- Ohnishi K, et al. Microprocessor-controlled DC motor for load-insensitive position servo system. IEEE Trans. Ind. Electron., 1987.（扰动观测器起源）
